
#Dikkat burada değerler anahtarlardan ayrı yazılır.
# port : yazdıktan sonra bir bboşluk atıp değeri yazmamız gerekiyor yani => port: 80

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/springdeneme
    username: postgres
    password: Telefon310
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
server:
  port: 80


